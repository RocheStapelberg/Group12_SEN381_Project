@using UkupholisaHealthcare.Library.Models
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
    //<input asp-action="Edit" asp-route-id="@provider.Id">Edit</input>
   //<input asp-action="Delete" asp-route-id="@provider.Id">Delete</input>
*@
@{
    Layout = "~/Views/Shared/_StaffLayout.cshtml";
}
@model List<Provider>
@{
    <div class="container">
        <h1 class="text-center">Provider Management</h1>
        <hr />
        <a class="col-2" asp-action="CreateProvider">Add new provider to database</a>
        <div class="row">
            <form class="form-inline" asp-action="Filter" >
                <div class="row mt-3">
                    <div class="col-3">
                        <label class="form-label" for="providerName">Search for a Provider:</label>
                    </div>
                    <div class="col-5">
                        <input class="form-control" type="text" name="providerName" required />
                    </div>
                    <div class="col-3">
                        <input class="btn btn-primary" type="submit" value="Search" />
                    </div>
                </div>
            </form>
        </div>
        <hr/>
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <td>Id</td>
                    <td>Provider Name</td>
                    <td>Provider Address</td>
                    <td>Provider Contact</td>
                    <td>Manage</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var provider in Model)
                {
                    <tr>
                        <td>@provider.Id</td>
                        <td>@provider.ProviderName</td>
                        <td>@provider.ProviderAddress</td>
                        <td>@provider.ProviderContact</td>
                        <td>
                            <input class="btn btn-outline-warning" type="submit" asp-controller="Provider" asp-route-id="@provider.Id" asp-action="Edit" value="Edit" />
                            <input class="btn btn btn-outline-danger" type="submit" asp-controller="Provider" asp-route-id="@provider.Id" asp-action="Delete" value="Delete" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}